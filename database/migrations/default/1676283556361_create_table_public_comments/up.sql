CREATE TABLE "public"."comments" ("id" serial NOT NULL, "user_id" integer NOT NULL, "text" text NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict);
